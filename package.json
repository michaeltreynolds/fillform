{
  "name": "fillform",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that fills FamilySearch Add Person / Add Parent forms from pasted CSV.",
  "scripts": {
    "build": "node scripts/build.mjs"
  }
}
